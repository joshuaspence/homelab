---
apiVersion: 'helm.fluxcd.io/v1'
kind: 'HelmRelease'
metadata:
  name: 'home-assistant'
  namespace: 'default'
  annotations:
    fluxcd.io/automated: 'true'
    fluxcd.io/tag.chart-image: 'semver:~0.'
spec:
  chart:
    repository: 'https://billimek.com/billimek-charts/'
    name: 'home-assistant'
    version: '1.0.3'
  releaseName: 'home-assistant'
  values:
    image:
      repository: 'homeassistant/home-assistant'
      tag: '0.113.3'
    ingress:
      enabled: true
      hosts:
      - 'home-assistant.spence.network'
    monitoring:
      enabled: true
      serviceMonitor:
        enabled: true
